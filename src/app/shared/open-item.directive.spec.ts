import { OpenItemDirective } from './open-item.directive';

describe('OpenItemDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenItemDirective();
    expect(directive).toBeTruthy();
  });
});
